import React from "react";
import ReactDOM from "react-dom/client";
import SignalDesk from "../App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SignalDesk />
  </React.StrictMode>
);
